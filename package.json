{
  "name": "genauigkeit-mono",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "pnpm -r --stream start:dev",
    "format": "pnpm -r --stream format",
    "lint": "pnpm -r --stream lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@9.1.4",
  "devDependencies": {
    "publint": "^0.2.8"
  }
}
